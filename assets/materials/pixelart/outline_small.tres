[gd_resource type="ShaderMaterial" load_steps=2 format=3]

; THE FINE LINE, for objects only a few pixels across -- buckets, tools, scattered pebbles. At the
; building weight these would be more line than object.

[ext_resource type="Shader" path="res://shaders/pixelart/pixel_outline.gdshader" id="1_shader"]

[resource]
shader = ExtResource("1_shader")
shader_parameter/outline_width = 0.014
shader_parameter/outline_color = Color(0.05, 0.042, 0.036, 1)
shader_parameter/use_baked_normal = true
